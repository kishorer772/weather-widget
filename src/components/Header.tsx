import { Typography } from '@mui/material';

export const Header = () => {
  return (
    <header>
      <Typography textAlign={'center'} variant="h4" sx={{ py: 4 }}>
        WidgetApp
      </Typography>
    </header>
  );
};
